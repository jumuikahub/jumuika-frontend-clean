// jest.config.js
export default {
  testEnvironment: "node",
  transform: {}, // no transforms yet — can add ts-jest later if needed
  moduleFileExtensions: ["js", "ts", "tsx"],
  roots: ["<rootDir>/tests"],
};
